import { PlaceholderPage } from '@/components/ui/PlaceholderPage';

// Module M2 — Flight Booking (SRS 3.3, FR-FLT-01 to 12)
export function FlightSearchPage() {
  return (
    <PlaceholderPage
      title="Flights"
      description="Search, compare, and book flights."
    />
  );
}
