import { PlaceholderPage } from '@/components/ui/PlaceholderPage';

// "My Trips" tab from the Figma bottom nav — distinct from Bookings;
// exact layout not yet captured from Figma (see docs/DEVELOPMENT_LOG.md).
export function TripsPage() {
  return <PlaceholderPage title="My Trips" description="Your upcoming and past trips." />;
}
