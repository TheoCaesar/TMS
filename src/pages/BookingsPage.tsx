import { PlaceholderPage } from '@/components/ui/PlaceholderPage';

// Wire to GET /bookings/me (src/lib/api/bookings.ts) once the Figma
// screen for this tab is captured — see docs/DEVELOPMENT_LOG.md.
export function BookingsPage() {
  return <PlaceholderPage title="Bookings" description="Your tour bookings and their status." />;
}
