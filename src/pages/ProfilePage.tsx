import { PlaceholderPage } from '@/components/ui/PlaceholderPage';

// Wire to GET /users/me + GET /users/me/loyalty (src/lib/api/users.ts)
// once the Figma screen for this tab is captured.
export function ProfilePage() {
  return <PlaceholderPage title="Profile" description="Your account, loyalty tier, and settings." />;
}
